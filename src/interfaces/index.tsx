export interface ITOAST_PRESET {
	position?: any,
	autoClose?: number,
	hideProgressBar?: boolean,
	pauseOnHover?: boolean,
}

export interface IUser {
	id: any,
	firstName: string,
	lastName: string,
	email: string,
	image: any
}